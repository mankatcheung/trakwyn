node_modules
dist
.output
.turbo
coverage
*.db
local.db.*
.pglite/
*.db-shm
*.db-wal
uploads/
.env
.env.local
.env.*.local
.env.production
.vercel
apps/api/src/index.js
apps/api/src/generated/
*.tsbuildinfo
storybook-static

# design-sync (see .design-sync/config.json / NOTES.md)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# graphify knowledge graph — generated, machine-specific paths inside;
# regenerate with /graphify. Findings live in Linear, not here.
graphify-out/

# agent scratch
.agents/

# Terraform (infra/gcp, infra/axiom) — state lives in GCS; tfvars hold per-install config
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
terraform.tfvars
